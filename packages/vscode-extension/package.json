{
  "name": "lzero-memory",
  "displayName": "L0 Memory",
  "description": "L0 / LanOnasis Memory-as-a-Service companion for VS Code",
  "version": "0.1.0",
  "publisher": "lanonasis",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/lanonasis/lzero-aether-mem.git"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": ["Other"],
  "main": "./out/extension.js",
  "activationEvents": [
    "onView:lzero.memorySidebar",
    "onCommand:lzeroMemory.authenticate"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "lzeroMemory",
          "title": "L0 Memory",
          "icon": "./media/icon.svg"
        }
      ]
    },
    "views": {
      "lzeroMemory": [
        {
          "id": "lzero.memorySidebar",
          "name": "L0 Memory",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "lzeroMemory.authenticate",
        "title": "L0 Memory: Authenticate"
      },
      {
        "command": "lzeroMemory.createMemoryFromSelection",
        "title": "L0 Memory: Create Memory from Selection"
      }
    ],
    "configuration": {
      "title": "LanOnasis",
      "properties": {
        "lanonasis.webAppUrl": {
          "type": "string",
          "default": "http://localhost:5001/vscode",
          "description": "Development web app URL used by the sidebar webview (IDEPanel preview)."
        },
        "lanonasis.apiUrl": {
          "type": "string",
          "default": "https://api.lanonasis.com/api/v1",
          "description": "Base URL for the LanOnasis Memory API."
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p .",
    "build:webview": "vite build --config vite.webview.config.ts",
    "dev": "tsc -p . --watch",
    "lint": "echo 'No lint configured for vscode-extension'",
    "typecheck": "tsc -p . --noEmit",
    "clean": "rm -rf out media/sidebar-react.* media/style.css"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.17",
    "@types/node": "^20.19.0",
    "@types/vscode": "^1.80.0",
    "@vitejs/plugin-react": "^5.1.1",
    "typescript": "^5.6.3",
    "vite": "^7.2.4"
  }
}
